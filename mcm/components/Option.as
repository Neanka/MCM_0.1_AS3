package mcm.components {
	
	/**
	 * Represents a single selection control that deselects any other option within the same group.
	 * Usually used as a group.
	 */
	public class Option extends Sprite {
		
	}
}